type Routes = {
  HOME: string;
  PRODUCT: string;
  CART: string;
  CHECKOUT: string;
  ORDERS: string;
};

export const routes: Routes = {
  HOME: "/merch",
  PRODUCT: "/merch/product",
  CART: "/merch/cart",
  CHECKOUT: "/merch/checkout",
  ORDERS: "/merch/orders",
};

export default routes;
