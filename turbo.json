{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "dev": {
      "cache": false,
      "env": ["PAYLOAD_SECRET", "MONGODB_URI", "WORDPRESS_API_URL", "PAYLOAD_PUBLIC_SERVER_URL", "PAYLOAD_PUBLIC_SERVER_PORT"]
    },
    "build": {
      "dependsOn": ["^build"],
      "env": ["PAYLOAD_SECRET", "MONGODB_URI", "WORDPRESS_API_URL", "PAYLOAD_PUBLIC_SERVER_URL", "PAYLOAD_PUBLIC_SERVER_PORT"],
      "outputs": ["dist/**", "build/**", "out/**", ".next/**"]
    },
    "build-storybook": {
      "dependsOn": ["^build"],
      "outputs": ["storybook-static"]
    },
    "start": {
      "dependsOn": ["build"],
      "env": ["PAYLOAD_SECRET", "MONGODB_URI", "WORDPRESS_API_URL", "PAYLOAD_PUBLIC_SERVER_URL", "PAYLOAD_PUBLIC_SERVER_PORT"]
    },
    "serve": {
      "dependsOn": ["build"],
      "env": ["PAYLOAD_SECRET", "MONGODB_URI", "WORDPRESS_API_URL", "PAYLOAD_PUBLIC_SERVER_URL", "PAYLOAD_PUBLIC_SERVER_PORT"]
    },
    "lint": {
      "outputs": []
    },
    "lint:fix": {
      "outputs": []
    },
    "test": {
      "outputs": []
    },
    "test:watch": {
      "cache": false
    },
    "cypress": {
      "outputs": []
    },
    "cypress:headless": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "cypress:start-headless": {
      "dependsOn": ["build"],
      "outputs": []
    }
  }
}
